// import { ChangeEventHandler } from "react";


export enum InputType {
  Password = "Password",
  Verification = "Verification",
}

export default interface Props {
    type?: boolean; //副标题（房间）不传则不显示
  }
  